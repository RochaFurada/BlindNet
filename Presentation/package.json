{
  "name": "blindnet-presentation",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "tsc && vite build",
    "preview": "vite preview --host 0.0.0.0",
    "bridge": "node bridge/server.mjs",
    "demo": "npm run build && node bridge/server.mjs"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "vite": "^5.4.11"
  },
  "dependencies": {
    "gsap": "^3.15.0"
  }
}
